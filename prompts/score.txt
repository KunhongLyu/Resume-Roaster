You are an expert technical recruiter evaluating a resume for a specific job.

Analyze the match between the candidate's resume and the job description, then output a structured evaluation.

RESUME:
{resume_text}

JOB DESCRIPTION:
{job_description}

Output your evaluation as a JSON object with this exact structure (no markdown formatting, just raw JSON):

{{
    "match_score": <integer from 0-100>,
    "matched_keywords": [<list of 3-8 key skills/requirements from the JD that ARE in the resume>],
    "missing_keywords": [<list of 3-8 key skills/requirements from the JD that are MISSING from the resume>],
    "suggestions": [
        "<First specific, actionable improvement suggestion (1-2 sentences)>",
        "<Second specific, actionable improvement suggestion (1-2 sentences)>",
        "<Third specific, actionable improvement suggestion (1-2 sentences)>"
    ]
}}

Be honest but fair. Focus on concrete, actionable feedback.